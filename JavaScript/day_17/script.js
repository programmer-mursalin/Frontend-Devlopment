let a = prompt('Enter  the fast number');
let b = prompt('Enter  the second number');

if (isNaN(a) || isNaN(b)) {
  throw SyntaxError('Sorry this is not allowed');
}
let sum = parseInt(a) + parseInt(b);
function main() {
  try {
    console.log('the number is', sum * x);
    return true;
  } catch (error) {
    console.log('error aa gaya hay');
    return false;
    7;
  } finally {
    console.log('solve');
  }
}
main();
